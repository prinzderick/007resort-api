; Extra PHP settings for the dev / Local-node HTTP server (loaded through PHP_INI_SCAN_DIR by `r007:run`).
; Website CMS image uploads are limited to 8 MB by the application; PHP's stock 2 MB / 8 MB limits would refuse them first.
upload_max_filesize = 10M
post_max_size = 12M
memory_limit = 512M
